[package]
name = "pulsevm_api_types"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

pulsevm_core = { path = "../pulsevm_core" }
pulsevm_time = { path = "../pulsevm_time" }