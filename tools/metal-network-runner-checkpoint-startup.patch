diff --git a/local/network.go b/local/network.go
--- a/local/network.go
+++ b/local/network.go
@@ -59 +59,5 @@ const (
-	processContextWaitTimeout   = 3 * time.Second
+	// A VM may need to restore a large imported state checkpoint before
+	// metalgo writes process.json. Keep this separate from the normal health
+	// cadence so snapshot-backed nodes are not reported as failed merely because
+	// startup legitimately takes longer than a few seconds.
+	processContextWaitTimeout   = 2 * time.Minute
